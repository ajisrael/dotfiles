#? Config file for btop v.1.4.7

#* Tokyo Night to match the palette already used by nvim/tmux/iTerm2/statusline.
color_theme = "tokyo-night"

#* Disabled so runtime tweaks (layout, sort order, etc.) don't get silently
#* written back into this tracked file - same problem as iTerm2's Rewritable
#* dynamic profiles (see dotfiles-amway's CLAUDE.md).
save_config_on_exit = false
